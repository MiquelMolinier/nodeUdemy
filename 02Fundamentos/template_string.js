const nombre = "Spyderman"
const real = "Peter Parker";
const union = nombre + " " + real;
const union2 = `${nombre} ${real}`
console.log(union2==union);
const lines = 
`linea1
linea2`
console.log(lines)